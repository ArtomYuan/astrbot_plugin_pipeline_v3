# 开发文档

## 架构（v0.4 最终版）

### 判断 → 链式生成 → 旁白收尾

```
用户消息
  → 判断层：轮流询问每个角色「这条消息是否涉及你」
      · 简洁 prompt：只问是否涉及（叫你/提及你/群体称呼包含你）——要求只答「是/否」
      · 解析：是 → 参与；否 → 静默交给下一个角色；非标准输出（角色误输出扮演文本）
        → 按「是」处理（宁可多回不可漏回——点名漏回是严重 bug）
      · 旁白独立判断——仅场景/动作/环境描述触发
  → 链式生成：参与角色按序，每个看到之前的完整对话
      角色A（用户消息）→ 角色B（用户消息+A）→ 角色C（用户消息+A+B）
      · 对话自然衔接（B 接 A 的话——不再各说各话）
  → 旁白收尾（每轮总是）：基于完整对话生成叙事
      · 必须包含：人物（动作/神态/状态）+ 地点（位置与环境）+ 事件（推进的事/氛围）
      · 输出 [旁白] + 固化 scene.json（场景唯一权威——防漂移）
```

### 事件处理

- `@filter.event_message_type(PRIVATE_MESSAGE, priority=100)` —— 只处理私聊
- `stop_event()` 终止事件传播（AngelHeart priority=-10 在 100 之后，不会介入）
- `event.send()` 直接发送（不走管道调度——避免异步竞态）
- **30s 超时保护**（`asyncio.wait_for`）——LLM 挂起不会卡死整个 Bot

## 设计决策与踩坑记录

| 版本 | 方案 | 结果 |
|:--|:--|:--|
| v1 | LLM 判断归属 + `yield chain_result` | ❌ 管道竞态——异步 LLM 返回时管道已结束，回复被丢弃 |
| v2 | 关键词匹配 + `event.send` | ❌ 多角色顺序响应不可靠（AngelHeart 预处理 + 管道生命周期干扰） |
| v3 | 判断/生成分离 + `llm_generate` 直调 | ✅ 绕过管道调度——但「各自独立生成」导致各说各话 |
| v4 | **链式生成 + 旁白收尾** | ✅ 角色间自然衔接 + 场景每轮固化 |

### 关键踩坑

1. **`llm_generate()` 必填 `chat_provider_id`**：漏传直接 TypeError。获取：`await context.get_current_chat_provider_id(umo)`
2. **插件 logger 是 `self.logger`**：`self.context.logger` 不存在——异常被吞表现为静默失败
3. **判断+生成合一的缺陷**：角色人设的「生成冲动」干扰「是否被叫」判定——轮流掉线。分离 + 严格 prompt（默认否）解决
4. **`thinking` 参数挂起**：DeepSeek 部分模型不支持 `thinking={"type":"disabled"}`——请求永久挂起（整个 Bot 无响应）——**移除该参数 + 加 30s 超时保护**（超时按无关处理，不卡死）
5. **各自独立生成的「各说各话」**：每个角色只看到用户消息——回应重复/冲突。**链式生成**（上下文累积传递）解决
6. **判断层输出扮演文本**：15K 人设让角色「忍不住演戏」——判断时输出角色扮演而非「是/否」——`startswith("是")` 解析失败导致点名漏回（Bot 无响应）。解法：判断 prompt 简化（只问是否涉及）+ 宽容解析（非标准输出按「是」——宁可多回不可漏回）
7. **AngelHeart 干扰**：priority=-10 被 100 压过 + `stop_event()` 终止传播——无需禁用
8. **持久化位置**：状态文件必须放 AstrBot data 目录（`plugin_data/`），放插件目录会被重装/更新覆盖
9. **群体称呼在场判定**：以**固化场景**中的在场角色为准（场景权威）——不依赖近期台词（台词少的角色不会被误判离场）

## 状态文件

位于 `<AstrBot>/data/plugin_data/astrbot_plugin_pipeline_v3/`：

| 文件 | 内容 |
|:--|:--|
| `scene.json` | 当前固化场景（旁白权威维护——每轮对话后更新） |
| `recent_lines.json` | 近期台词历史（滑动窗口——判断上下文） |

## 配置（WebUI 插件设置）

| 配置项 | 说明 | 默认 |
|:--|:--|:--|
| `persona_ids` | 参与演出的角色人格 ID，逗号分隔 | 空（使用内置默认） |
| `judge_provider_id` | 判断层模型（AstrBot provider ID） | `deepseek_talk/deepseek-v4-flash` |
| `judge_temperature` | 判断层温度（越低越确定） | `0` |

## 更新记录

每次改动在文末追加记录并更新版本号（当前版本见 metadata.yaml）。

### v0.4.0（2026-08-09）——链式生成 + 旁白收尾 + 判断简化（当前）
- 判断层：简洁 prompt（只问是否涉及）+ 宽容解析（非标准输出按「是」——防点名漏回）
- 生成层：链式生成（角色按序，上下文累积传递——对话自然衔接）
- 旁白收尾：每轮生成叙事（人物动作/地点环境/事件氛围）+ 固化 scene.json
- 30s 超时保护（防 LLM 挂起卡死）；移除 thinking 参数（DeepSeek 兼容问题）

### v0.3.0（2026-08-09）——场景固化
- 旁白为场景唯一权威——每轮对话后固化场景状态（scene.json）
- 所有角色判断/生成注入当前场景——防场景漂移

### v0.2.0（2026-08-09）——判断/生成分离
- 判断层与生成层分离（判断用轻量配置——temperature=0）
- 轮流询问各角色「是否在叫你」

### v0.1.0（2026-08-09）——骨架
- 初始骨架：路由判断 + 人格复用 + 状态持久化框架

## 开发流程

1. 本地修改 → 提交推送（GitHub）
2. 部署机插件目录 `git pull`
3. 重启 AstrBot（或 WebUI 重载插件）
4. 日志排查：`docker logs astrbot-core | grep pipeline_v3`

## 路线图

- [ ] 记忆系统（由 AstrBot 插件生态承担——插件不特指/不绑定）
- [ ] 判断层模型可换（OpenAI 兼容端点直连）
